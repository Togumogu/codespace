#include <stdio.h>
#include <cs50.h>

int main(void)
{
    long test = get_long("number?");

    int set1 = 0;
    long tmp = test ;
    int digit = 0;

    do
    {
        digit = (test % 100) / 10;

    }
    while (tmp > 0)




    printf("%lu\n", calc1);
    printf("%lu\n", calc2);
    printf("%lu\n", calc3);
}